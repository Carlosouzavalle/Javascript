// Operadores:

// Crie uma função chamada soma que recebe dois números como argumentos e retorna a soma deles.
// Crie uma função chamada multiplicacao que recebe dois números como argumentos e retorna o resultado da multiplicação deles.

function soma(a , b) {
    return a + b
}

console.log(soma(2, 2));



const multi = (c, d) => {
    return c * d
}

console.log(multi(3, 5))